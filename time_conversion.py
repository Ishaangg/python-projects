#Display time

seconds = int(input("Enter an integer for seconds:"))

minutes = seconds // 60

remaining_seconds = seconds % 60

print(f"seconds {seconds} have {minutes} minutes and {remaining_seconds} seconds" )

